import 'package:flutter/material.dart';
import 'package:aplikasi_teachme/main.dart';

class Pengajar_detail extends StatelessWidget {
  const Pengajar_detail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pengajar detail'),
      ),
      body: const Center(
        child: Text('Pengajar detail'),
      ),
    );
  }
}